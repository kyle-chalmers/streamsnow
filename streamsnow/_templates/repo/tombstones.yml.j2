# Tombstone registry for {{ project_name }} (generated by StreamSnow).
#
# The deploy pipeline only ever runs CREATE OR REPLACE STREAMLIT — it has no
# delete path. When an app is renamed or removed, add its ABANDONED deployed
# identifier here in the SAME PR: CI blocks a PR that abandons an identifier
# without a tombstone (`streamsnow check tombstones`), and the deploy
# workflow's reconcile step drops every identifier listed below
# (`streamsnow check tombstones --drop-sql`).
#
# Entry shape:
#   - identifier: {{ app_database }}.{{ app_schema }}.SOME_RETIRED_APP
#     reason: renamed to SOME_NEW_APP
#     date: 2026-01-01
#
# This file is yours — StreamSnow scaffolds it once and never rewrites it.
tombstones: []
