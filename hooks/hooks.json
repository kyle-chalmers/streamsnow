{
  "//": "StreamSnow Claude Code hooks. SessionStart emits a one-line skill pointer, guarded to fire only inside a StreamSnow repo (see hooks/session_start.sh).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.sh\""
          }
        ]
      }
    ]
  }
}
