---
description: Deprecated v0.2 alias — use /review-app --auto instead. Will be removed in the next major release.
---

# /auto-review-app (merged into /review-app in v0.3)

The review→fix loop is now `/review-app --auto`. Invoke the `review-app` skill with `--auto` and the same arguments: $ARGUMENTS
