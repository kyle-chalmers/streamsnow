---
description: Deprecated v0.2 alias — use /audit-lineage instead. Will be removed in the next major release.
---

# /deep-dive-data (renamed to /audit-lineage in v0.3)

Live-DB lineage and column-fidelity checking is now `/audit-lineage`. Invoke the `audit-lineage` skill with the same arguments: $ARGUMENTS
