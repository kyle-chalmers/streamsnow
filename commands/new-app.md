---
description: Deprecated v0.2 alias — use /start-app instead. Will be removed in the next major release.
---

# /new-app (merged into /start-app in v0.3)

Scaffolding is now the scaffold phase of `/start-app`. Invoke the `start-app` skill with the same arguments: $ARGUMENTS
