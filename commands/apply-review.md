---
description: Deprecated v0.2 alias — use /review-app --fix instead. Will be removed in the next major release.
---

# /apply-review (merged into /review-app in v0.3)

Applying review findings is now `/review-app --fix`. Invoke the `review-app` skill with `--fix` and the same arguments: $ARGUMENTS
