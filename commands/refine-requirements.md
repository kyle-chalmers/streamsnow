---
description: Deprecated v0.2 alias — use /start-app --spec instead. Will be removed in the next major release.
---

# /refine-requirements (merged into /start-app in v0.3)

Writing or backfilling a REQUIREMENTS.md spec is now the spec phase of `/start-app`. Invoke the `start-app` skill with `--spec` and the same arguments: $ARGUMENTS
