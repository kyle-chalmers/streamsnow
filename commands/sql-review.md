---
description: Deprecated v0.2 alias — use /review-app --sql instead. Will be removed in the next major release.
---

# /sql-review (merged into /review-app in v0.3)

Writing the sql_review/ companions is now `/review-app --sql`. Invoke the `review-app` skill with `--sql` and the same arguments: $ARGUMENTS
