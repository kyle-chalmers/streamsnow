---
description: Deprecated v0.2 alias — use /start-app instead. Will be removed in the next major release.
---

# /add-page (merged into /start-app in v0.3)

Adding a page is now the build phase of `/start-app`, which resumes into it for an existing app. Invoke the `start-app` skill with the same arguments: $ARGUMENTS
