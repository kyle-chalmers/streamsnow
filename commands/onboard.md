---
description: Deprecated v0.2 alias — use /start-app --setup instead. Will be removed in the next major release.
---

# /onboard (merged into /start-app in v0.3)

Machine + repo setup is now `/start-app --setup`. Invoke the `start-app` skill with `--setup` and the same arguments: $ARGUMENTS
